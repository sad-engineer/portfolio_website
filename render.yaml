services:
  - type: web
    name: portfolio
    env: python
    rootDir: apps/portfolio
    buildCommand: pip install poetry && poetry install --without dev --no-interaction --no-ansi
    startCommand: poetry run uvicorn portfolio.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
